using SimpleCQRS.UAM.Domain.Services;
using System.Security.Claims;
using UAM.WebService.Configuration;

namespace UAM.Domain.Test
{
    public class TokenServiceTest
    {
       /* [Fact]
        public void Test1()
        {
            JwtBearerTokenSettings jwtBearerTokenSettings = new JwtBearerTokenSettings();
            jwtBearerTokenSettings.CertificatePath = @"D:\private.key";
            jwtBearerTokenSettings.Issuer = "www.self.com";

            TokenService tokenService = new TokenService(jwtBearerTokenSettings);

            List<Claim> claims = new List<Claim>();
            claims.Add(new Claim("Role", "Admin"));
            var token = tokenService.GenerateToken(claims);
        }*/
    }
}